import 'package:flutter/material.dart';

class TshirtsScreen extends StatelessWidget {
  const TshirtsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
